function [D, D_labels, Y, Y_Labels] = extract_digits(digits, K, N)
% EXTRACT_DIGITS Load K training samples and N testing samples of each digit in c
%   [D, D_labels, Y, Y_Labels] = EXTRACT_DIGITS(digits, K, N)